---
import SearchBar from "./SearchBar.jsx";

// Fetch data on the server side
const digest = await fetch("http://localhost:8080/digest");
const tag = await fetch("http://localhost:8080/tags");
const digestData = await digest.json();
const tagData = await tag.json();
---

<SearchBar client:load blogArr={digestData} tags={tagData} />